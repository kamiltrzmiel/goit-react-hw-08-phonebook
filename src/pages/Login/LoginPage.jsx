import LoginForm from 'components/Login';
import { Section } from './LoginPage.styled';

const LoginPage = () => {
  return (
    <Section>
      <LoginForm />
    </Section>
  );
};

export default LoginPage;
